fun main() {
    print(fib(3));
    print(fib(4));
    print(fib(10));
}

fun fib(n: int) {
    if(n == 0) {
        return 0;
    }
    if(n == 1) {
        return 1;
    }
    return fib(n - 1) + fib(n - 2);
}